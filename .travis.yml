language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-kosmoport' ]; then cd .. && eval "mv $currentfolder generator-kosmoport" && cd generator-kosmoport; fi
